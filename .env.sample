# --- Database ---
DATABASE_URL=postgresql://your_username@localhost:5432/language_training

# --- Auth (JWT) ---
# Generate with: openssl rand -base64 64 | tr -d '\n'
# Access and refresh MUST be different secrets.
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# --- Email (Resend) ---
RESEND_API_KEY=
EMAIL_FROM=

# --- Payload CMS ---
# Any random string. Used to sign Payload's admin session cookies.
PAYLOAD_SECRET=
# Email of the first admin user created on initial boot.
INITIAL_ADMIN_EMAIL=

# --- Speech-check service ---
# HS256 secret shared with the language-licks-speech-check gateway.
# Must match language-licks-speech-check/gateway/.env byte-for-byte.
SPEECH_CHECK_JWT_SECRET=
# Public URL of the speech-check gateway (http://localhost:8080 in dev).
NEXT_PUBLIC_SPEECH_CHECK_GATEWAY_URL=

# --- Paddle (billing) ---
PADDLE_API_KEY=
PADDLE_WEBHOOK_SECRET=
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=
NEXT_PUBLIC_PADDLE_PRICE_ID=
# "sandbox" for dev, "production" for prod.
NEXT_PUBLIC_PADDLE_ENV=sandbox

# --- Google Cloud Storage (media uploads, optional in dev) ---
GCS_PROJECT_ID=
GCS_BUCKET=
GCS_CREDENTIALS=
